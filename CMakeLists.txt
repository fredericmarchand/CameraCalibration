cmake_minimum_required(VERSION 2.8)
project( cameraCalibration )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( cameraCalib projection-template.cpp )
target_link_libraries( cameraCalib ${OpenCV_LIBS} )
